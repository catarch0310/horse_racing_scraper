name: Diagnose Broken Scrapers

# 手動觸發
on:
  workflow_dispatch:

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 lxml

      - name: Run diagnostic
        run: python diagnose_broken_scrapers.py

      - name: Upload diagnose output as artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: diagnose-output
          path: diagnose_output/
          retention-days: 30

      - name: Commit diagnose output back to repo
        if: success()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add diagnose_output/
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: update diagnose output for broken scrapers"
            git push
          fi
